import { SortAuthorsPipe } from './sort-authors.pipe';

describe('SortAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
